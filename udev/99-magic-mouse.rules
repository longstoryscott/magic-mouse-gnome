# Magic Mouse 2 - Allow user access to HID raw device
# This enables running the gesture driver without root privileges
#
# Format: KERNELS matches the parent HID device path
# 0005 = Bluetooth bus, 004C = Apple vendor ID, 0269 = Magic Mouse 2 product ID

KERNEL=="hidraw*", KERNELS=="0005:004C:0269.*", MODE="0666"
